// src/types/snarkjs.d.ts
declare module 'snarkjs' {
  // you can refine these `any` types later if you like
  export const groth16: any;
  // if you need other exports (e.g. `bigintify`), add them here too
}
